package dev.norska.clt.api;

import dev.norska.clt.ClearLagTimer;

public class ClearLagTimerAPI {
	
	/** 
	* Set the interval (does not affect ClearLagg's interval).
	* You can get the instance of ClearLagTimer with ClearLagTimer.getInstance();.
	* @param ClearLagTimer Instance, int
	*/
	
	public static void setInterval(ClearLagTimer instance, int interval) {
		
	}
	
	/** 
	* Get the interval.
	* You can get the instance of ClearLagTimer with ClearLagTimer.getInstance();.
	* @param ClearLagTimer Instance
	* @return int
	*/
	
	public static int getInterval(ClearLagTimer instance) {
		return 0;
	}

}
